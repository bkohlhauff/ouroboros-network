steps:

  - label:   'slow RealTPraos tests'
    command: '.buildkite/slow-RealTPraos.sh'
    agents:
      queue:  benchmark     # so this runs alone on a dedicated "machine"
      system: x86_64-linux

  - label:   'validate mainnet'
    command: '.buildkite/validate-mainnet.sh'
    agents:
      system: x86_64-linux
